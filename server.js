const express = require('express');
// const {MongoClient} = require('mongogb');
const cors = require();

const app = express();

// config mongo db
// const MONGO_URL = 'mongodb://localhost:27017';
// const DB_NAME = 'meteo_db';

// let db;

// middleware

app.use(cors());
app.use(express.json());

// connexion à mongodb


